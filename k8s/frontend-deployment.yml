apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: mychat-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      name: frontend-pod
      namespace: mychat-app
      labels: 
        app: frontend
    spec:
      containers:
      - name: chatapp-frontend
        image: onkar1817/frontendimg
        ports:
        - containerPort: 80
        env:
        - name: NODE_ENV
          value: production
          



